import { Slot } from "vue";

/**
 * Slots for the Action Panel component
 */
export interface IdsActionPanelSlots {
  /**
   * The default slot content.
   * @slot default
   */
  default?: Slot<unknown>;
}