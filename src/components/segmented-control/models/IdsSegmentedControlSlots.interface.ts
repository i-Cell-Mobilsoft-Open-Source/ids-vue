import { Slot } from "vue";

/**
 * Slots for the Segmented Control component
 */
export interface IdsSegmentedControlSlots {
  /**
   * The default slot content.
   * @slot default
   */
  default?: Slot<unknown>;
}